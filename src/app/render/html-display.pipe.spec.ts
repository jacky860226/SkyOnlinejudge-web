import { HtmlDisplayPipe } from './html-display.pipe';

describe('HtmlDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
