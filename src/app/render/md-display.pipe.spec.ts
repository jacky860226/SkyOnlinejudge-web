import { MdDisplayPipe } from './md-display.pipe';

describe('MdDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new MdDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
